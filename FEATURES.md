# ThreadPilot for ChatGPT 功能特点总结

## 项目定位

ThreadPilot for ChatGPT 是一个面向 ChatGPT 网页端长对话场景的浏览器扩展。它的目标不是改变 ChatGPT 的回答方式，而是改善长对话中的阅读、定位、回看和整理体验，让用户在复杂线程里更快找到需要的内容。

## 用户痛点

- 长对话内容越来越多，靠滚轮查找某一段内容效率很低。
- AI 回复较长时，页面被单条回复占满，用户很难快速浏览上下文。
- 想回看自己提出过的问题或 AI 某次关键回答时，需要反复上下滚动。
- ChatGPT 页面自带控件在部分场景下容易与阅读区域或扩展控件重叠。
- 切换到已有对话时，用户通常更关心最新内容，但页面不一定自动定位到底部。

## 核心功能

### 侧边对话导航

ThreadPilot 会在 ChatGPT 页面侧边生成一条轻量导航栏，把长对话整理成可点击的消息节点。用户可以通过节点快速了解整段对话的结构，并直接跳转到对应位置。

### 悬浮消息预览

鼠标悬停在导航节点上时，会显示对应消息的简短预览。用户可以在跳转前先判断这条消息是否是自己要找的内容，减少误跳和重复滚动。

### 快速跳转与拖动定位

导航栏支持点击节点跳转，也支持拖动浏览长对话。对于几十轮甚至上百轮的长线程，这个功能可以显著降低定位成本。

### 聚焦我的输入

导航栏默认只显示用户自己的输入节点。长对话里，用户通常是沿着自己的问题、指令和补充信息回看上下文，这样可以减少 AI 长回复造成的节点噪音，也不需要额外切换视图。

### AI 长回复折叠

ThreadPilot 可以自动折叠较长的 AI 回复，并为每条长回复提供独立的展开 / 收起控制。用户自己的输入会保持完整可见，方便在长回复之间快速浏览对话上下文。

### 新对话自动定位到底部

打开或切换到一个已有对话时，ThreadPilot 可以自动定位到最新消息附近。这个功能适合继续处理历史对话，减少每次进入长线程后手动滚到底部的操作。

### 隐藏原生导航控件

扩展支持隐藏 ChatGPT 页面中可能产生干扰的原生导航控件，减少界面重叠，让侧边导航区域更清爽。

### 可调显示偏好

用户可以在扩展弹窗中调整常用偏好，包括：

- 是否启用导航条
- 是否隐藏原生导航
- 是否启用 AI 长回复折叠
- 是否打开新对话自动定位到底部
- 导航栏显示在左侧或右侧
- 导航栏侧边距离
- 折叠后保留高度
- 长对话流畅模式

## 快捷键

- `Alt + J`：显示 / 隐藏导航栏
- `Alt + C`：开启 / 关闭 AI 长回复折叠
- `Alt + N`：跳到下一条消息
- `Alt + P`：跳到上一条消息

## 隐私与数据

ThreadPilot 只使用浏览器本地存储保存扩展偏好设置。它不上传用户对话内容，不使用远程分析，不插入广告，也不依赖外部服务处理聊天内容。

## 适用场景

- 使用 ChatGPT 进行长时间研究、写作、编程或资料整理。
- 需要频繁回看历史问题和回答。
- 一个对话中包含多轮方案讨论、代码修改、需求迭代或资料总结。
- 希望减少长回复带来的页面占用，提高阅读和定位效率。

## 项目链接

- 官网：https://billowssun.github.io/threadpilot-for-chatgpt/
- GitHub：https://github.com/billowssun/threadpilot-for-chatgpt
- 最新版本：https://github.com/billowssun/threadpilot-for-chatgpt/releases/latest
