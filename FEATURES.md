# ThreadPilot 3.0.1 产品说明

## 定位

ThreadPilot 面向经常处理长 ChatGPT 对话的人。它不扩展 ChatGPT 的能力，只降低两个阅读成本：

1. 长回答占满页面。
2. 很难回到之前的某一轮。

因此产品只保留“对话折叠”和“对话时间线”。

## 对话折叠

- 兼容 ChatGPT 当前使用的 `section[data-testid="conversation-turn-*"]` 消息结构。
- 页面只加载部分历史消息时，开头的孤立 AI 回答仍可折叠和导航。
- 默认只自动折叠生成完成的长回答。
- 折叠后保留回答开头、字数和展开入口。
- 每条回答都能手动展开或收起。
- 独立批量按钮根据当前状态切换“折叠全部 / 展开全部”，不占用时间线点击层。
- 批量操作会保持当前问题在视口中的位置，时间线轮次不会跳走。
- 用户手动展开后，后续 DOM 更新不会再次自动收起。
- 折叠强度分为紧凑、平衡和宽松三档。

## 对话时间线

- 每轮只使用用户提问生成一个位置刻度。
- 刻度按消息在对话中的真实位置映射，不再平均排布。
- 当前轮次随页面滚动更新。
- 悬停节点可预览提问、回答和折叠状态。
- 点击刻度会把对应问题准确放到固定顶栏下方，并短暂高亮。
- 轨道两端提供回到顶部和跳到底部按钮。
- 支持拖动浏览、上一轮和下一轮快捷键。
- 超长对话自动压缩为密集刻度，轨道本身不产生第二层滚动。
- 窄屏自动压缩，不覆盖正文。

## 运行与性能

- 流式生成期间只增量更新最后一条 AI 回答，不再反复扫描整页。
- 与消息无关的 ChatGPT 页面更新会被忽略，减少长对话卡顿。
- 扩展弹窗显示当前 ChatGPT 页面的连接状态、版本、轮次数和折叠数。
- 页面未连接时可从弹窗直接重新加载并恢复扩展。

## 快捷键

- `Alt + J`：显示或隐藏时间线
- `Alt + C`：折叠或展开全部回答
- `Alt + N`：下一轮
- `Alt + P`：上一轮

## 隐私

- 不请求网络权限。
- 不上传或持久化对话正文。
- 只保存两个开关和折叠强度。

## 详细规格

参见 [重构规格](docs/REFACTOR_SPEC.md)。
